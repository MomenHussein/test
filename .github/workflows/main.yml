name: hello_project
on:
  push:
    branches:
      - main
jobs:
  test_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print_hello
        run: |
          echo "Hello World"
          cd . .
          cat README.md
    
